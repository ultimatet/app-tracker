export default {
    content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
    theme: {
        extend: {},
    },
    plugins: [],
};
// This is a Tailwind CSS configuration file for an Electron app.
// It specifies the content paths to scan for class names, extends the default theme, and includes